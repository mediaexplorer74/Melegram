<Page
    x:Class="TelegramClient.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TelegramClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="768"
    d:DesignWidth="480">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="WELCOME TO MELEGRAM!" FontSize="28" FontWeight="Bold" Foreground="Blue" HorizontalAlignment="Center"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <TextBlock x:Name="PageTitle" TextAlignment="Center" Text="Melegram"
                       FontSize="60" Margin="0,50,0,0" FontWeight="Bold" Foreground="White" VerticalAlignment="Top" />
            <Ellipse Height="171" Width="169" HorizontalAlignment="Center" Margin="0,150,0,0" VerticalAlignment="Top">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="ApplicationIcon.png" Stretch="UniformToFill" />
                </Ellipse.Fill>
            </Ellipse>
            <TextBlock x:Name="ApplicationTitle"
                       TextAlignment="Center"
                       FontSize="20"
                       Margin="0,350,0,0"
                       Foreground="White"
                       VerticalAlignment="Top">
                <Run Text="Welcome to Melegram - A Telegram Client" />
                <LineBreak />
                <Run Text=" for Windows Phone 7, Made by ChockingNetDude" />
                <LineBreak />
                <Run Text=" for Windows 10 Mobile, Made by [M]edia[E]xplorer" />
                <LineBreak />
                <LineBreak />
                <!--Run Text=" It's free and long-term supported by me!" />
                <LineBreak />
                <Run Text=" App Version: 2.1" /-->
            </TextBlock>
            <Button Content="START MESSAGING" 
                    Height="80" HorizontalAlignment="Center" 
                    Margin="0,155,0,0" Name="button1" 
                    VerticalAlignment="Top" Width="450" 
                    Click="button1_Click" 
                    Background="Lime" 
                    Foreground="Black" 
                    FontSize="24" 
                    FontWeight="Bold"/>
        </Grid>
    </Grid>

</Page>